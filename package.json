{
  "name": "formproject",
  "version": "1.0.0",
  "description": "This is an employment application form for a fictional company.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run clean && npm run imagemin && npm run copyfonts && npm run usemin",
    "start": "npm run lite",
    "lite": "lite-server"
  },
  "repository": {
    "type": "git"
  },
  "author": "Tessie Clements",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.6.1"
  }
}
